{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cmake: build -DCMAKE_BUILD_TYPE=Debug",
            "type": "shell",
            "command": "cmake --build ${workspaceFolder}/build --config Debug",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$msCompile"
        }
    ]
}
